<div class="container p-5 w-50 bg-light shadow rounded my-2">
  <h2 class= "text-center mb-3">Bienvenido a Eventik <i class="fa-solid fa-face-smile" style="color: #5C5C5C;"></i></h2>

  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), data: { turbo: :false, controller: "disable-form"}) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs lh-lg">
      <%= f.input :name,
                  required: true,
                  label: "Nombre"%>
      <%= f.input :last_name,
                  required: false,
                  label: "Apellido"%>
      <%= f.input :user_name,
                  required: true,
                  label: "Nombre de usuario"%>
      <%= f.input :avatar,
                  as: :file,
                  required: false,
                  label: "Foto de perfil"%>
      <%= f.input :email,
                  required: true,
                  autofocus: true,
                  input_html: { autocomplete: "email" }%>
      <%= f.input :password,
                  label: "Contraseña",
                  required: true,
                  hint: ("#{@minimum_password_length} caracteres minimos" if @minimum_password_length),
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :password_confirmation,
                  label: "Confirmá tu contraseña",
                  required: true,
                  input_html: { autocomplete: "new-password" } %>


      <%= f.input :supplier,
                  as: :boolean,
                  label: "Quiero ofrecer un servicio",
                  required: false,
                  input_html: { data: {disable_form_target:"checkbox", action: "click->disable-form#show" }},
                  wrapper_html: { id: "checkbox", type:"checkbox"}%>


      <%= f.input :business_name,
            label: "Nombre de tu Empresa",
            label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
            required: true,
            input_html: { data: {disable_form_target:"fields"}, class: 'hidden'} %>



      <%= f.input :service_type,
                  label: "Tipo de Servicio",
                  label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
                  required: true,
                  input_html: { data: {disable_form_target:"fields"}, class: 'hidden'},
                  label_html_options: {class: 'hidden'},
                  :collection => ["Fotografía", "Catering", "Espacios", "Entretenimiento", "Invitaciones & Decoración", "Disfraces & Accesorios"],
                  :prompt => "Elegí la categoría que representa a tu servicio" %>
      <%= f.input :address,
                  label: "Dirección",
                  label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
                  required: true,
                  input_html: { data: {disable_form_target:"fields"}, class: 'hidden'} %>
      <%= f.input :photos,
                  as: :file,
                  label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
                  input_html: {multiple: true, data: {disable_form_target:"fields"}, class: 'hidden'},
                  required: false,
                  label: "Fotos del servicio"%>
      <%= f.input :rut,
                  label: "Registro Único Tributario",
                  label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
                  required: false,
                  input_html: { data: {disable_form_target:"fields"}, class: 'hidden'} %>
      <%= f.input :description,
                  label: "Descripccion de la empresa",
                  label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
                  input_html: { data: {disable_form_target:"fields"}, class: 'hidden'},
                  required: true %>
      <%= f.input :duration,
                  label: "Duración del servicio",
                  label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
                  input_html: { data: {disable_form_target:"fields"}, class: 'hidden', autocomplete: "El servicio se brinda por x horas"},
                  required: false %>
      <%= f.input :capacity,
                  label: "Capacidad del servicio",
                  label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
                  input_html: { data: {disable_form_target:"fields"}, class: 'hidden', autocomplete: "Indicá el número de personas"},
                  required: false %>
      <%= f.input :includes,
                  label: "¿Qué está incluido en el servicio?",
                  label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
                  input_html: { data: {disable_form_target:"fields"}, class: 'hidden'},
                  required: false %>
      <%= f.input :benefits,
                  label: "Beneficios adicionales del servicio",
                  label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
                  input_html: { data: {disable_form_target:"fields"}, class: 'hidden', autocomplete: "De regalo con el servicio..."},
                  required: false %>
      <%= f.input :notes,
                  label: "Notas adicionales del servicio",
                  label_html: { data: {disable_form_target:"fields"}, class: 'hidden' },
                  input_html: { data: {disable_form_target:"fields"}, class: 'hidden', autocomplete: "Documentación para el ingreso..."},
                  required: false %>
    </div>

    <div class="form-actions d-flex mb-3">
      <%= f.button :submit, "Sign up", class: "btn btn-success text-light flex-grow-1" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
