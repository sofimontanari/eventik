<div class="container pb-4 mt-4">
  <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item"><%= link_to "Inicio", root_path, class: "text-decoration-none text-success" %></li>
      <li class="breadcrumb-item active" aria-current="page">Categorías de Eventos</li>
    </ol>
  </nav>

  <h5 class="fw-bold mb-4 pt-2"> Categorías de Eventos de <%= @user.business_name %> : </h5>
    <% if @user.event_types.find_by(name:"Cumpleaños Infantiles") %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Cumpleaños Infantiles</p>
        <i class="fa-solid fa-check me-2" style="color: #FF3BB8;"></i>
      </div>
    <% else %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Cumpleaños Infantiles</p>
        <%= button_to "", type_path(@user), method: :patch, class: "fa-solid fa-plus btn btn-success text-light", params: {event_type: "Cumpleaños Infantiles"}, data: {toggle:'tooltip'}, title:"Agregar categoría"%>
      </div>
    <% end %>

    <% if @user.event_types.find_by(name:"Cumpleaños Adultos") %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Cumpleaños Adultos</p>
        <i class="fa-solid fa-check" style="color: #FF3BB8;"></i>
      </div>
   <% else %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Cumpleaños Adultos</p>
        <%= button_to "", type_path(@user), method: :patch, class: "fa-solid fa-plus btn btn-success text-light", params: {event_type: "Cumpleaños Adultos"}, data: {toggle:'tooltip'}, title:"Agregar categoría"%>
      </div>
    <% end %>

    <% if @user.event_types.find_by(name:"Despedidas") %>
     <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Despedidas</p>
        <i class="fa-solid fa-check" style="color: #FF3BB8;"></i>
      </div>
    <% else %>
     <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Despedidas</p>
        <%= button_to "", type_path(@user), method: :patch, class: "fa-solid fa-plus btn btn-success text-light", params: {event_type: "Despedidas"}, data: {toggle:'tooltip'}, title:"Agregar categoría"%>
      </div>
    <% end %>

    <% if @user.event_types.find_by(name:"Casamientos") %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Casamientos</p>
        <i class="fa-solid fa-check" style="color: #FF3BB8;"></i>
      </div>
    <% else %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Casamientos</p>
        <%= button_to "", type_path(@user), method: :patch, class: "fa-solid fa-plus btn btn-success text-light", params: {event_type: "Casamientos"}, data: {toggle:'tooltip'}, title:"Agregar categoría"%>
      </div>
    <% end %>

    <% if @user.event_types.find_by(name:"Bautismos") %>
     <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Bautismos</p>
        <i class="fa-solid fa-check" style="color: #FF3BB8;"></i>
      </div>
    <% else %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Bautismos</p>
        <%= button_to "", type_path(@user), method: :patch, class: "fa-solid fa-plus btn btn-success text-light", params: {event_type: "Bautismos"}, data: {toggle:'tooltip'}, title:"Agregar categoría"%>
      </div>
    <% end %>

    <% if @user.event_types.find_by(name:"Baby Showers") %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Baby Showers</p>
        <i class="fa-solid fa-check" style="color: #FF3BB8;"></i>
      </div>
    <% else %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">Baby Showers</p>
        <%= button_to "", type_path(@user), method: :patch, class: "fa-solid fa-plus btn btn-success text-light", params: {event_type: "Baby Showers"}, data: {toggle:'tooltip'}, title:"Agregar categoría"%>
      </div>
    <% end %>

    <% if @user.event_types.find_by(name:"15 Años") %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">15 Años</p>
        <i class="fa-solid fa-check" style="color: #FF3BB8;"></i>
      </div>
    <% else %>
      <div class="d-flex justify-content-start align-items-center border-bottom w-50">
        <p class="flex-grow-1 align-self-center pt-3">15 Años</p>
        <%= button_to "", type_path(@user), method: :patch, class: "fa-solid fa-plus btn btn-success text-light", params: {event_type: "15 Años"}, data: {toggle:'tooltip'}, title:"Agregar categoría"%>
      </div>
    <% end %>

    </div>
      <div class="container mt-1 pt-2 mb-3 d-flex justify-content-start">
      <%= link_to "Volver", root_path, class: "btn btn-ghost" %>
    </div>

</div>
